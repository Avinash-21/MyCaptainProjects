#include<stdio.h>
main()
{
	float a,b;
	printf("Enter the first number:");
	scanf("%f",&a);
	printf("Enter the second number:");
	scanf("%f",&b);

	/*Swapping 2 numbers*/
	printf("After Swapping successfully the results are ....\n");
	printf("First number is %f\n",b);
	printf("Second number is %f",a);
	return 0;
}
